import '../../../core/app_export.dart';/// This class is used in the [thirtyfourchipview_item_widget] screen.
class ThirtyfourchipviewItemModel {ThirtyfourchipviewItemModel({this.categories, this.isSelected, }) { categories = categories  ?? "Design & Creative";isSelected = isSelected  ?? false; }

String? categories;

bool? isSelected;

 }
