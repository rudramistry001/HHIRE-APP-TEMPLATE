import '../../../core/app_export.dart';/// This class is used in the [jobschipview_item_widget] screen.
class JobschipviewItemModel {JobschipviewItemModel({this.jobs, this.isSelected, }) { jobs = jobs  ?? "Part Time";isSelected = isSelected  ?? false; }

String? jobs;

bool? isSelected;

 }
